# wrangler.toml — 放在仓库根
name = "ip-geoaddress-generator"
compatibility_date = "2025-12-01"
compatibility_flags = ["nodejs_compat"]

# next-on-pages 生成的静态输出目录（next-on-pages 默认会输出到 .vercel/output/static）
pages_build_output_dir = ".vercel/output/static"
